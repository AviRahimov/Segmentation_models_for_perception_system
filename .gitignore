# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
build/
develop-eggs/
dist/
eggs/
.eggs/
*.egg-info/
*.egg
wheels/
pip-wheel-metadata/
share/python-wheels/

# Virtual environments
.venv/
.venv-rfdetr-train/
.venv-inpaint/
venv/
env/
ENV/
.python-version

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Type-checking / linting caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# Logs
*.log
logs/

# PyTorch / model weights
*.pt
*.pth
# TorchScript serialized models (e.g. distilled YOLOE text encoders)
*.ts
*.onnx
*.engine
*.trt
*.plan
# Use weights/* (not weights/) so the hf_assets exception below can work
weights/*
# Track tiny HF config JSONs for offline SegFormer startup (cache_hf_assets.py)
!weights/hf_assets/
checkpoints/

# Datasets (heavy) — leading slash = root-level only, not src/perception/datasets/
# Use /datasets/* (not /datasets/) so subdirectory exceptions can work
/datasets/*
/data/
!/datasets/detection/
/datasets/detection/*
# Track detection dataset placeholder README only (not the data files themselves)
!/datasets/detection/Detection_Dataset/
/datasets/detection/Detection_Dataset/*
!/datasets/detection/Detection_Dataset/README.md
!/datasets/segmentation/
/datasets/segmentation/*
*.zip
*.tar
*.tar.gz

# Outputs
output/
outputs/
runs/
*.mp4.part

# Hugging Face cache override (project-local)
.hf_cache/
cache/

# Jupyter
.ipynb_checkpoints/

# Project-local samples / media (heavy)
/samples

# Environment / secrets
.env
.env.*
# Use /reports/* (not /reports) so subdirectory exceptions can work
/reports/*
# Track placeholder dirs so collaborators see intended structure
!/reports/segmentation/
/reports/segmentation/*
!/reports/segmentation/.gitkeep
!/reports/detection/
/reports/detection/*
!/reports/detection/.gitkeep
# Training provenance log (see CLAUDE.md) — small, append-only, meant to be durable/shared
!/reports/detection/experiments.jsonl
